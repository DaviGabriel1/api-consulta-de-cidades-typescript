# passo-a-passo para instalação de dependencias (apenas para desenvolvimento)

- `npm init --y`
- `npm install express dotenv`
- `npm install -D typescript @types/express ts-node-dev`

# configurar arquivo de build:
`"outDir": "./build"`
